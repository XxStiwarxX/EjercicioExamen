#include <iostream>
int main() {
    int x = 5, y = 1;
    if (x > 0) {
        printf("El numero es positivo");
    } else {
        for (int i = y++; i < x; ++i) {
            y = x * i;
        }
        printf("El factorial de %d es %d", x, y);
    }
}
